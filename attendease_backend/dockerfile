# Use Tomcat 9 base image with JDK 17 (works well with WARs)
FROM tomcat:9.0-jdk17

# Remove default apps
RUN rm -rf /usr/local/tomcat/webapps/*

# Copy your WAR to Tomcat webapps
COPY target/attendease_backend.war /usr/local/tomcat/webapps/ROOT.war

# Expose port 8080 (Render will map automatically)
EXPOSE 8080

# Start Tomcat
CMD ["catalina.sh", "run"]
